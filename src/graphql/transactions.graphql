query Transactions($date: DateTime) {
  transactions (date: $date) {
    id
    amount
    currency
    date
    description
    categoryId
    exchangeRate {
      id
      base
      date
      rates
    }
  }
}