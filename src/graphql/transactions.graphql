query Transactions($startDate: DateTime!, $endDate: DateTime!) {
  transactions (startDate: $startDate, endDate: $endDate) {
    id
    amount
    currency
    date
    description
    categoryId
    exchangeRate {
      id
      base
      date
      rates
    }
    planningId
  }
}