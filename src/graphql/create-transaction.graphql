mutation CreateTransaction($transactionData: TransactionInput!) {
  createTransaction(transactionData: $transactionData) {
    id
    amount
    currency
    date
    description
    categoryId
    exchangeRate {
      id
      base
      date
      rates
    }
    planningId
  }
}
