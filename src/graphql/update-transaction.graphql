mutation UpdateTransaction ($transactionId: Float!, $transactionData: TransactionInput!) {
  updateTransaction(transactionData: $transactionData, id: $transactionId) {
    id
    amount
    categoryId
    date
    description
    exchangeRate {
      id
      base
      date
      rates
    }
  }
}