mutation Login($email: String!, $password: String!) {
  signIn(signInData: { email: $email, password: $password }) {
    accessToken
    refreshToken
    user {
      id
      name
      email
      lang
      currency
      onboardingAt
      monthlyBudget
      monthStartDay
    }
  }
}
