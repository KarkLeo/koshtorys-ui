mutation Registration($name: String!, $email: String!, $password: String!, $lang: String!) {
  signUp(signUpData: { name: $name, email: $email, password: $password, lang: $lang }) {
    id
    name
    email
    lang
  }
}
