# Base URL of the koshtorys-api REST endpoint.
# Inlined into the bundle at build time — not read at runtime.
VITE_API_ENDPOINT=http://localhost:3000/api

# Optional: only needed to publish Storybook to Chromatic.
# CHROMATIC_PROJECT_TOKEN=
# STORYBOOK_CHROMATIC_PROJECT_ID=
